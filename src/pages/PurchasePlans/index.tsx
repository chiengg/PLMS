import { FeatureMarker } from '@/components/FeatureMarker';
import PlansFilter from './components/PlansFilter';
import PlansTable from './components/PlansTable';

export default function PurchasePlans() {
  return (
    <div className="flex flex-col h-full">
      <FeatureMarker 
        requirementNumber={4}
        title="面包屑导航" 
        description={`**功能描述：** 展示当前页面位置的层级导航，帮助用户了解当前所在位置并快速跳转。

**导航路径：**
- 首页 > 采购流程 > 采购计划

**交互说明：**
- 点击「首页」跳转至系统首页
- 点击「采购流程」跳转至采购流程主页面
- 点击「采购计划」刷新当前页面
- 当前页面名称以深色高亮显示（灰色800）

**显示样式：**
- 文字颜色：未激活状态灰色500，当前页面灰色800
- 分隔符：">"符号，间距mx-2
- 字体大小：13px

**业务规则：**
- 面包屑层级最多显示3层
- 始终显示完整路径`}
      >
        <div className="flex items-center text-gray-500 text-[13px] mb-4">
          <span>首页</span>
          <span className="mx-2">{'>'}</span>
          <span>采购流程</span>
          <span className="mx-2">{'>'}</span>
          <span className="text-gray-800 font-medium">采购计划</span>
        </div>
      </FeatureMarker>
      
      <FeatureMarker 
        requirementNumber={5}
        title="筛选区" 
        description={`**功能描述：** 支持多维度搜索与筛选采购计划数据，帮助采购员快速定位目标计划。

**搜索类型（单选切换）：**
- **库存SKU**：按SKU编码搜索
- **商品名称**：按中文名称搜索
- **采购计划号**：按计划编号搜索
- **备注**：按备注内容搜索
- **申请人**：按申请人姓名搜索
- **采购员**：按采购员姓名搜索

**筛选条件：**
- **仓库**：下拉选择，默认选中「东莞厚街仓」
- **计划来源**：全部、采购建议、手动创建、批量导入
- **采购状态**：全部、未采购、已采购
- **删除原因**：全部、有、无

**交互说明：**
- 搜索框支持双击批量查询（输入多个关键词用空格分隔）
- 点击「更多」按钮展开更多筛选条件
- 点击「重置」按钮清空所有筛选条件
- 搜索结果实时更新

**显示样式：**
- 单选按钮组位于搜索框左侧
- 筛选条件横向排列
- 「更多」按钮展开后显示额外条件

**业务规则：**
- 搜索类型切换后，搜索框placeholder提示对应类型
- 仓库筛选支持多选（预留扩展）
- 计划来源标识数据录入方式`}
      >
        <PlansFilter />
      </FeatureMarker>
      
      <FeatureMarker 
        requirementNumber={6}
        title="计划列表" 
        description={`**功能描述：** 展示已生成的采购计划，按采购员分组显示，支持批量操作和详情查看。

**分组结构：**
- **组头（灰色背景）**：显示计划号、采购员、商品数量、建议采购数量、采购总价
- **组内商品**：库存SKU、中文名称、供应商、仓库/仓位、采购数量、入库量/损耗量、备注、物流信息、采购状态、计划来源、创建人、创建时间

**交互特性：**
- 支持批量勾选商品进行批量操作
- 支持编辑采购数量和备注
- 支持更换供应商（弹出选择弹窗）
- 支持更换采购员（组级别操作）
- 支持生成采购单（单个或批量选中）
- 支持搁置计划（单个或批量）
- 支持展开/折叠分组

**采购状态：**
- 🟠 **橙色标签「未采购」**：计划已创建但未生成采购单
- 🟢 **绿色标签「已采购」**：已生成采购单并发送给供应商
- 其他状态按业务规则扩展

**计划来源：**
- **采购建议**：从采购建议页面加入的计划
- **手动创建**：手动添加的计划
- **批量导入**：通过Excel文件导入的计划

**业务规则：**
- 一个计划可以包含多个SKU
- 同一个计划内的SKU通常来自同一供应商
- 采购总价 = Σ(采购单价 × 采购数量)
- 入库量 + 损耗量 ≤ 采购数量

**显示样式：**
- 分组表格样式，组头灰色背景
- 商品行白色背景，交替行可添加浅色背景
- 操作按钮位于每行末尾
- 支持横向滚动查看完整信息`}
      >
        <PlansTable />
      </FeatureMarker>
    </div>
  );
}
